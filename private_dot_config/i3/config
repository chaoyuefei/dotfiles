
# i3 Configuration File
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.

# Please refer to https://i3wm.org/docs/userguide.html for complete reference!

# Set Mod key (Mod1 corresponds to the Alt key)
set $mod Mod1

# Fonts Configuration
font pango:monospace 8
# Alternative font option (commented out)
# font pango:DejaVu Sans Mono 8
# Set the border color for focused, focused inactive, and unfocused windows
# class                 border  backgr. text    indicator
client.focused          #ffffff #90ee90 #ffffff #00ff00
client.focused_inactive #333333 #5f676a #ffffff #484e50
client.unfocused        #333333 #222222 #888888 #292d2e
client.urgent           #2f343a #900000 #ffffff #900000
new_window pixel 5
hide_edge_borders vertical

# Startup Programs
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id bash ~/.config/polybar/launch.sh --blocks
exec --no-startup-id xrandr --output eDP-1 --mode 1920x1200


bindsym $mod+shift+l exec "i3lock -i /home/chaoyue/wallpapers/Green_MW_Mac.png"
exec --no-startup-id nm-applet
exec --no-startup-id feh --bg-scale /home/chaoyue/wallpapers/Green_MW_Mac.png

exec --no-startup-id rclone mount dropbox: ~/Dropbox/
focus_follows_mouse no


# Background and Wallpaper
bindsym $mod+shift+w exec feh --bg-scale /home/chaoyue/wallpapers/Green_MW_Mac.png

# Volume Control
bindsym $mod+v exec --no-startup-id pavucontrol

# Application Launchers
bindsym $mod+Tab exec rofi-gpaste
# bindsym $mod+b exec rofi-bluetooth
# bindwsym $mod+w exec networkmanager_dmenu
bindsym $mod+n exec /usr/local/bin/networkmanager-dmenu/networkmanager_dmenu

# Workspace Navigation
bindsym Mod1+k workspace next
bindsym Mod1+j workspace prev

# Audio Control using PulseAudio
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Window Management
floating_modifier $mod
# bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec "/usr/bin/flatpak run --branch=stable --arch=x86_64 --command=wezterm org.wezfurlong.wezterm start --cwd ."
bindsym $mod+Shift+q kill
# bindsym $mod+shift+space exec "rofi -combi-modi window,drun,ssh,run,filebrowser -show combi -show-icons"
# bindsym $mod+space exec "rofi -modi drun,run -show drun"
bindsym $mod+shift+space exec "rofi -combi-modi window,drun,ssh,run,filebrowser -show combi -show-icons -theme ~/.config/polybar/blocks/scripts/rofi/launcher.rasi"
bindsym $mod+space exec "rofi -no-config -no-lazy-grab -show drun -modi drun -theme ~/.config/polybar/blocks/scripts/rofi/launcher.rasi"

# Monitor Management
# bindsym $mod+m exec xrandr --output DisplayPort-0 --auto --above eDP --output eDP --auto
# bindsym $mod+shift+m exec xrandr --output DisplayPort-0 --output eDP --off --auto 
# bindsym $mod+l exec xrandr --output eDP --mode 1920x1080 --auto --output DisplayPort-0 --auto --above eDP
bindsym $mod+m exec /usr/local/bin/display_setting.bash
bindsym Mod4+m exec bash /home/chaoyue/Matlab/2023b/bin/matlab -desktop
bindsym $mod+Shift+m exec /usr/local/bin/monitor_settings/dual_monitors_v1.sh 

# Directional Focus Control
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move Focused Window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Container Layout Management
bindsym $mod+t layout stacking
bindsym $mod+e layout toggle split
bindsym Control+space floating toggle

# Screenshots
bindsym $mod+s exec flameshot gui
bindsym $mod+shift+s exec flameshot full

# Workspace Naming
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Workspace Switching
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move Focused Container to Workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# Reload and Restart i3
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+r exec subl .config/i3/config
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# Bar Configuration
# bar {
#     status_command i3status
#     font pango:monospace 8, FontAwesome 10
#     position top
#     colors {
#         background #000000
#         statusline #76d7c4
#         separator  #888888
#     }
# }
# exec --no-startup-id bash ~/.config/polybar/launch.sh --blocks
bindsym $mod+p exec --no-startup-id bash ~/.config/polybar/blocks/preview.sh
bindsym $mod+b exec killall polybar
bindsym $mod+Shift+b exec --no-startup-id bash ~/.config/polybar/launch.sh --blocks
# bindsym $mod+Shift+b exec --no-startup-id bash ~/.config/polybar/blocks/launch.sh


# Disable Tab Title
font pango:Ubuntu Bold 0
for_window [class=".*"] title_format " "

